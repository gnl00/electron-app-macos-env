name: Build/release te-app

on:
  push:
    tags:
      - v*.*.*

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-22.04, macos-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install -g node-gyp && npm install -g node-pre-gyp && npm install -g pnpm && pnpm install

      - name: build-linux
        if: matrix.os == 'ubuntu-22.04'
        run: sudo apt install -y libx11-dev libxtst-dev libc6 libstdc++6 build-essential && npm rebuild && pnpm build:linux

      - name: build-mac
        if: matrix.os == 'macos-latest'
        run: npm rebuild && pnpm build:mac

      - name: build-win
        if: matrix.os == 'windows-latest'
        run: npm rebuild && pnpm build:win

      - name: release
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          files: |
            dist/*.exe
            dist/*.zip
            dist/*.dmg
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm
            dist/*.tar.gz
            dist/*.yml
            dist/*.blockmap
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
